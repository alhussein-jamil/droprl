# Cassie task deps (install core first: make install)
# numpy re-pinned here because mujoco may try to upgrade it.
numpy==1.26.4
mujoco==3.3.1
numba==0.61.2
scipy==1.15.3
opencv-python-headless==4.11.0.86
